import './Promise.css';

export default function Promise() {
  return (
    <section className="promise">
      <div className="promise-inner">
        <div className="promise-quote">
          Public transport doesn't have to feel like <span>chaos.</span>
        </div>
        <p className="promise-sub">
          Wabebe is built for everyone who's ever waited too long, fought for a seat, or wondered if the bus was even coming. We bring the system the sector has needed for years.
        </p>
      </div>
    </section>
  );
}